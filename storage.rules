rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /tracks/{allPaths=**} {
      allow read, write: if request.auth != null;
    }

    match /layers/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
