rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth!=null;
    }

    match /public/{imageId} {
      allow read: if resource.size < 10000 * 1024;
      allow write: if true;
    }
  }
}
